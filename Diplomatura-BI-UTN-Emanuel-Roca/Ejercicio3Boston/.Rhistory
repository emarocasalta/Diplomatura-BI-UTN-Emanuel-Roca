#Carga de librerias
install.packages("corrplot")
library(MASS)
library(shiny)
library(tidyverse)
library(ggplot2)
library(skimr)
library(corrplot)
#Carga de los datos
data(Boston)
Boston
#Buscamos correlaciones con la  variable "medv"
correlation_medv <- cor(Boston)[, "medv"]
#Excluimos la variable "medv"
correlation_medv <- correlation_medv[correlation_medv != 1]
#Filtramos las variables que tengan una correlacion > 0.5
selected_var <- names(correlation_medv)[abs(correlation_medv) > 0.5]
selected_var
#Agregamos la variable "medv"
selected_var <- c("medv", selected_var)
#Realizamos la regresion lineal
lm_medv <- lm(medv ~ ., data = Boston_num[, selected_var])
#Desarrollar la ecuacion del modelo en formato texto
equation_medv <- paste("medv =", paste0(coefficients[1], " + ", paste0(coefficients[-1], " * ", names(coefficients[-1]), collapse = " + ")) )
#Desarrollar la ecuacion del modelo en formato texto
equation_medv <- paste("medv =", paste0(coefficients[1], " + ", paste0(coefficients[-1], " * ", names(coefficients[-1]), collapse = " + ")) )
#Buscamos correlaciones con la  variable "medv"
correlation_medv <- cor(Boston)[, "medv"]
#Excluimos la variable "medv"
correlation_medv <- correlation_medv[correlation_medv != 1]
#Filtramos las variables que tengan una correlacion > 0.5
selected_var <- names(correlation_medv)[abs(correlation_medv) > 0.5]
selected_var
#Agregamos la variable "medv"
selected_var <- c("medv", selected_var)
#Realizamos la regresion lineal
lm_medv <- lm(medv ~ ., data = Boston_num[, selected_var])
#Agregamos la variable "medv"
selected_var <- c("medv", selected_var)
#Realizamos la regresion lineal
lm_medv <- lm(medv ~ ., data = Boston_num[, selected_var])
#Agregamos la variable "medv"
selected_var <- c("medv", selected_var)
#Realizamos la regresion lineal
lm_medv <- lm(medv ~ ., data = Boston_num[, selected_var])
#Agregamos la variable "medv"
selected_var <- c("medv", selected_var)
#Realizamos la regresion lineal
lm_medv <- lm(medv ~ ., data = Boston[, selected_var])
summary(lm_medv)
#Obtenemos los coeficientes del modelo
coefficients <- coef(lm_medv)
#Desarrollar la ecuacion del modelo en formato texto
equation_medv <- paste("medv =", paste0(coefficients[1], " + ", paste0(coefficients[-1], " * ", names(coefficients[-1]), collapse = " + ")) )
print(equation_medv)
#Asignar valores a las variables del modelo
rm <- 6.0
ptratio <- 20.0
lstat <- 10.0
#Calcular "medv"
medv_predicho <- 18.5671115053955 + 4.51542094385541 * rm + -0.93072255527063 * ptratio + -0.571805687872475 * lstat
#Imprimir resultado
print(medv_predicho)
runApp('Ejercicio3Boston')
load("C:/Users/acer/Videos/Diplomatura BI UTN/Unidad 3/Diplomatura-BI-UTN-Emanuel-Roca/Ejercicio3Boston/.RData")
shiny::runApp()
